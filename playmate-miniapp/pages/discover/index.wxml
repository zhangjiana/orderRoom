<view class="page">
  <view class="row"><text class="eyebrow">PLAY TOGETHER / 一起玩</text><text class="link" bindtap="profile">家庭资料 ›</text></view>
  <view class="hero"><text class="badge">让快乐，发生在家附近</text><view class="title">小小的周末，
大大的玩心。</view><view class="sub">找几个合拍的小朋友，
把周末还给一起玩的时光。</view><view class="hero-art">🪁</view></view>
  <view class="notice">本地体验版 · 以下为示例活动，报名不会通知真实家庭。地点与家庭数据仅用于体验。</view>
  <view class="row"><view class="section">发现好玩的事</view><text class="link" bindtap="toggleAge">{{ageFilter ? '✓ ' + age + '岁适合' : '筛选适合年龄'}}</text></view>
  <view class="filters"><view wx:for="{{categories}}" wx:key="*this" class="chip {{category===item?'active':''}}" data-value="{{item}}" bindtap="filter">{{item}}</view></view>
  <view class="card" wx:for="{{items}}" wx:key="id" data-id="{{item.id}}" bindtap="open">
    <view class="row"><view class="icon-tile">{{item.symbol}}</view><text class="badge">{{item.state}} · {{item.remaining > 0 ? '还可报名' : '已满员'}}</text></view>
    <view class="card-title">{{item.title}}</view><view class="meta">{{item.date}} · {{item.time}} · 约 1 小时</view><view class="meta">{{item.location}}</view>
    <view class="divider"/><view class="row"><text class="sub">{{item.minAge}}～{{item.maxAge}} 岁 · {{item.count}}/{{item.capacity}} 个家庭</text><text class="link">一起玩 ›</text></view>
  </view>
  <view class="card empty" wx:if="{{!items.length}}"><view class="empty-icon">🌱</view><view class="card-title">还没有合适的约玩</view><view class="sub">你可以成为第一个发起的人。</view><button class="primary" bindtap="publish">发起一个约玩</button></view>
  <view class="card"><view class="card-title">有个想玩的点子？</view><view class="sub">骑车、玩球、看云，都值得一起。</view><button class="secondary" bindtap="publish">＋ 发起约玩</button></view>
  <view class="footer-note">家长陪同 · 公共场所 · 轻松约玩</view>
</view>
